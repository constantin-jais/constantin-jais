# Open Questions — rumble-canvas

| Question | Impact | Owner | Status |
| --- | --- | --- | --- |
| Is the first target user solo builder or team? | High | Product | Open |
| Is the canonical unit called `canvas`, `project`, or `spec workspace`? | High | Product/Architecture | Open |
| What is the minimum viable screen representation: list, table, graph, or visual map? | Medium | Product | Open |
| Should Markdown be the canonical storage format or only an export format? | High | Architecture | Accepted: structured fields are canonical; Markdown is projection/export. |
| How much AI assistance is allowed before human validation? | High | Product/Security | Open |
| What is the first Bolt handoff format? | High | Bolt | Accepted: planning-only `ImplementationHandoff` using `canvas.bolt_handoff.v0.1`, targeting `cos-matic` for MVP. |
| Should `TraceabilityLink` be first-class in MVP? | High | Product/Architecture | Accepted. |
| Which spec completeness checks belong in Wrench? | Medium | Wrench | Open |
